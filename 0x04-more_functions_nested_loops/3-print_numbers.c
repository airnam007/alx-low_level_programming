#include "main.h"

/**
 * print_numbers - Displays numbers
 * Return: 0(Success)
 */

void print_numbers(void)
{
	int a = 0;

	while (a < 10)
	{
		_putchar(a);
		a++;
	}
	_putchar('\n');

	return (0);
}
