#include "main.h"

/**
 * print_numbers - Displays numbers
 * Return: 0(Success)
 */

void print_numbers(void)
{
	int c = 0;

	while (c < 10)
	{
		_putchar(c);
		c++;
	}
	_putchar('\n');

	return (0);
}
