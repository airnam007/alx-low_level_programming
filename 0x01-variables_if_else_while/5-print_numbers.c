#include <stdio.h>

/**
 * main - Displays all values of base 10 with for
 *
 * Return: 0(Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
